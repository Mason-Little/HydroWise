type CourseActionResultsProps = {
  query: string;
};

// Stub: wire course switching here; enable section divider in ActionResultsPanel when needed.
export function CourseActionResults(_props: CourseActionResultsProps) {
  return null;
}
