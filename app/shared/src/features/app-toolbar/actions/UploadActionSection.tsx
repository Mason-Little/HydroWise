type UploadActionSectionProps = {
  query: string;
};

// Stub: action-bar upload UI; dashboard upload stays separate.
export function UploadActionSection(_props: UploadActionSectionProps) {
  return null;
}
